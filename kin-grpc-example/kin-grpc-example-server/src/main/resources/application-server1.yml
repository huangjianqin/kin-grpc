server:
  port: 10001

spring:
  application:
    name: grpc-server
  cloud:
    nacos:
      discovery:
        register-enabled: true
        server-addr: localhost:8848

grpc:
  server:
    port: 10011

instanceId: 1